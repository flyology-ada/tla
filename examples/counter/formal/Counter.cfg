\* Explore the behavior predicate and require every reachable state to retain
\* the finite typing relation consumed by Ada generation.
SPECIFICATION Spec

INVARIANTS
    TypeOK
    WitnessPending

CHECK_DEADLOCK FALSE

\* Alias is the reviewed machine-readable projection emitted in TLC traces.
ALIAS Alias
